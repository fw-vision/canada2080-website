<a class="dynamic-wordmark" href="/" aria-label="Canada2080 home">
  <span class="wordmark-canada" aria-hidden="true">Canada</span>
  <span class="wordmark-year" aria-hidden="true">2080</span>
  <span class="wordmark-stroke" aria-hidden="true"></span>
</a>

<style>
  .dynamic-wordmark {
    position: relative;
    display: inline-flex;
    align-items: baseline;
    min-width: 8.5rem;
    padding: 0.15rem 0.4rem 0.2rem 0;
    color: var(--ink);
    text-decoration: none;
    isolation: isolate;
  }

  .wordmark-canada {
    font-family: 'Birthstone', 'Snell Roundhand', 'Segoe Script', cursive;
    font-size: 2.15rem;
    font-weight: 400;
    line-height: 0.7;
    letter-spacing: 0.01em;
    transform: rotate(-2deg);
    transform-origin: 80% 70%;
    transition: transform 500ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .wordmark-year {
    margin-left: -0.12rem;
    font-family: 'Caveat', 'Segoe Print', cursive;
    font-size: 1.35rem;
    font-weight: 500;
    line-height: 1;
    letter-spacing: 0.03em;
    transform: translateY(0.18rem) rotate(1deg);
    transition: transform 500ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .wordmark-stroke {
    position: absolute;
    right: 0.2rem;
    bottom: -0.3rem;
    left: 0.25rem;
    height: 0.5rem;
    border-top: 1px solid currentColor;
    border-radius: 50%;
    transform: scaleX(0.72) rotate(-1deg);
    transform-origin: left;
    transition: transform 550ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .dynamic-wordmark:hover {
    color: var(--ink);
  }

  .dynamic-wordmark:hover .wordmark-canada,
  .dynamic-wordmark:focus-visible .wordmark-canada {
    transform: translateY(-0.1rem) rotate(-1deg);
  }

  .dynamic-wordmark:hover .wordmark-year,
  .dynamic-wordmark:focus-visible .wordmark-year {
    transform: translate(0.12rem, 0.1rem) rotate(-1deg);
  }

  .dynamic-wordmark:hover .wordmark-stroke,
  .dynamic-wordmark:focus-visible .wordmark-stroke {
    transform: scaleX(1) rotate(-1deg);
  }

  @media (prefers-reduced-motion: reduce) {
    .wordmark-canada,
    .wordmark-year,
    .wordmark-stroke {
      transition: none;
    }
  }
</style>
